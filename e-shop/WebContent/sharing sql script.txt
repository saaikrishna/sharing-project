CREATE TABLE  "NEW2" 
   (	"VENDOR_ID" VARCHAR2(50) NOT NULL ENABLE, 
	"FIRST_NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"LAST_NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"AGE" VARCHAR2(50) NOT NULL ENABLE, 
	"GENDER" VARCHAR2(50) NOT NULL ENABLE, 
	"CONTACT" VARCHAR2(50) NOT NULL ENABLE, 
	"ADDRESS" VARCHAR2(50) NOT NULL ENABLE, 
	"CITY" VARCHAR2(50) NOT NULL ENABLE, 
	"ZIP_CODE" VARCHAR2(50) NOT NULL ENABLE, 
	"LANDLINE" VARCHAR2(50) NOT NULL ENABLE, 
	"EMAIL" VARCHAR2(50) NOT NULL ENABLE, 
	"CATEGORY" VARCHAR2(50) NOT NULL ENABLE, 
	 PRIMARY KEY ("VENDOR_ID") ENABLE
   ) ;CREATE TABLE  "PRODUCT" 
   (	"P_ID" VARCHAR2(20) NOT NULL ENABLE, 
	"P_NAME" VARCHAR2(20) NOT NULL ENABLE, 
	"P_PRICE" VARCHAR2(20) NOT NULL ENABLE, 
	"DESCRIPTION" VARCHAR2(50) NOT NULL ENABLE, 
	"P_VENDOR_ID" VARCHAR2(20) NOT NULL ENABLE
   ) ;CREATE TABLE  "SERVICE" 
   (	"SERVICE_ID" VARCHAR2(50) NOT NULL ENABLE, 
	"SERVICE_NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"SERVICE_PRICE" VARCHAR2(50) NOT NULL ENABLE, 
	"SERVICE_DESCRIPTION" VARCHAR2(50) NOT NULL ENABLE, 
	"SERVICE_VENDOR_ID" VARCHAR2(50) NOT NULL ENABLE
   ) ;CREATE TABLE  "USER1" 
   (	"FIRST_NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"LAST_NAME" VARCHAR2(50) NOT NULL ENABLE, 
	"AGE" VARCHAR2(50) NOT NULL ENABLE, 
	"GENDER" VARCHAR2(50) NOT NULL ENABLE, 
	"CONTACT_NUMBER" VARCHAR2(50) NOT NULL ENABLE, 
	"USER_ID" VARCHAR2(50) NOT NULL ENABLE, 
	"PASSWORD" VARCHAR2(50) NOT NULL ENABLE, 
	"ROLE" VARCHAR2(50) NOT NULL ENABLE
   ) ;ALTER TABLE  "PRODUCT" ADD FOREIGN KEY ("P_VENDOR_ID")
	  REFERENCES  "NEW2" ("VENDOR_ID") ENABLE;ALTER TABLE  "SERVICE" ADD FOREIGN KEY ("SERVICE_VENDOR_ID")
	  REFERENCES  "NEW2" ("VENDOR_ID") ENABLE;